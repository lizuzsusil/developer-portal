---
id: auth
title: Authentication Module (sdk.auth)
sidebar_label: sdk.auth
---

import {CodeTabs, LanguageProvider} from '@site/src/components/CodeTabs';

<LanguageProvider>

# Authentication (`sdk.auth`)

Exposes the host's authenticated user to the mini app. Read-only - login itself happens in the host; this module only reports who is signed in and ends the session on request.

---

## Public Methods

### `getUser()`

Returns the currently authenticated user, or `null` when nobody is signed in.

**Signature:** `getUser(): Promise<PlatformUser | null>`

**Returns:** `Record<string, unknown> | null`. The user object is host-defined - its keys vary between shells, so don't code against a fixed field list. Read what you need defensively (`user?.name ?? "there"`) and narrow at runtime.

<CodeTabs
  typescript={`const user = await sdk.auth.getUser();
if (!user) {
  showSignInPrompt();
} else {
  greet((user.name as string | undefined) ?? "Welcome, User");
}`}
  javascript={`const user = await sdk.auth.getUser();
if (!user) {
  showSignInPrompt();
} else {
  greet(user?.name ?? "there");
}`}
/>

### `isAuthenticated()`

Whether a user is currently signed in. Prefer this for gating UI; use `getUser()` when you need the profile.

**Signature:** `isAuthenticated(): Promise<boolean>`

<CodeTabs
  typescript={`if (await sdk.auth.isAuthenticated()) {
        {**Do Something**}
}`}
  javascript={`if (await sdk.auth.isAuthenticated()) {
        {**Do Something**}
}`}
/>

### `logout()`

Ends the host session. The host also emits an auth event so other mini apps can reconcile.

**Signature:** `logout(): Promise<void>`

<CodeTabs
  typescript={`await sdk.auth.logout();
{**your next action**}`}
  javascript={`await sdk.auth.logout();
{**your next action**}`}
/>

</LanguageProvider>
