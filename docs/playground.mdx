---
id: playground
title: Interactive SDK Playground
sidebar_label: Interactive SDK Playground
---

import { SDKExplorer } from "@site/src/components/SDKExplorer";

# Interactive SDK Playground

The explorer below is part of the portal itself - a live documentation widget in `packages/sdk-playground` that lets you test any public SDK method, inspect parameter shapes, and copy code snippets. It is not a standalone installable package; it runs here as a documentation aid.

- **Modules covered:** `auth`, `permissions` (deprecated), `flags`, `config`, `navigation` (incl. `router`), `storage`, `platform`, `device`, `api` (unary + `stream: true`), `notifications`, `links`, `appearance`
- **Removed:** `http` and `gicChat` no longer exist in the SDK - network goes through `sdk.api.request`, chat streams via `stream: true` + `sdk.stream.parseSseStream`.
- **Source:** `packages/sdk-playground/src/SDKExplorer.tsx` inside the portal monorepo
- **Design tokens:** `packages/design-tokens/tokens.css`

<SDKExplorer />
